export type IContractInfo = {
  totalDeposited: number;
  totalBonus: number;
};

export type IDepositStats = {
  plan: number;
  percent: number;
  amount: number;
  start: number;
  finish: number;
};
